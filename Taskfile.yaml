---
# yaml-language-server: $schema=https://taskfile.dev/schema.json

version: 3

output: prefixed
interval: 3s

vars:
  default: production
  workspaces:
    - production

set:
  # Enforce the pipefail option in the Bash library to ensure the failure of any one step in the pipeline fails all.
  - pipefail

# NOTE: The `flatten` option is used when including other Taskfiles to ensure that the tasks are included at the
#       top-level of this Taskfile. This is important to allow tasks in the included Taskfiles to be called directly
#       from other imported Taskfiles, simplifying the overall structure of the Taskfiles and allowing for a more
#       modular approach to task management.

includes:
  utils:
    taskfile: .task/utils.yaml
    flatten: true
  terraform:
    taskfile: .task/terraform.yaml
    dir: terraform
    flatten: true
  tfswitch:
    taskfile: .task/tfswitch.yaml
    dir: terraform
    flatten: true
  tflint:
    taskfile: .task/tflint.yaml
    dir: terraform
    flatten: true
  prettier:
    taskfile: .task/prettier.yaml
    flatten: true
  yamllint:
    taskfile: .task/yamllint.yaml
    flatten: true
  markdownlint:
    taskfile: .task/markdownlint.yaml
    flatten: true
  check-jsonschema:
    taskfile: .task/check-jsonschema.yaml
    flatten: true
  actionlint:
    taskfile: .task/actionlint.yaml
    flatten: true
  zizmor:
    taskfile: .task/zizmor.yaml
    flatten: true
  snyk:
    taskfile: .task/snyk.yaml
    flatten: true

tasks:
  default:
    desc: Run the linting, formatting, validation, analysis, and documentation tasks
    summary: |-
      Run the linting, formatting, validation, analysis, and documentation tasks for all Terraform configurations and
      associated files. Use the following command for additional information on the steps involved:

        $ task --summary develop
    silent: true
    cmds:
      - task: develop

  develop:
    aliases:
      - d
      - dev
    desc: Run the linting, formatting, validation, analysis, and documentation tasks
    summary: |-
      Run the linting, formatting, validation, analysis, and documentation tasks for all Terraform configurations and
      associated files.

      The `develop` task is designed to run the core integration steps for this repository (and can do so continuously
      when using the `--watch` option). It is designed to be run during development to ensure that any changes made to
      the code are valid, correctly formatted, and do not introduce any security issues or misconfigurations.

      Specifically, it will format and lint all supported files, validate the Terraform configuration, run static code
      analysis tools, and keep the documentation up-to-date, where it is automated.
    deps:
      - task: utils:pre-commit
      - task: utils:pre-checks
    silent: true
    watch: true
    cmds:
      - defer: "{{ if .EXIT_CODE }}echo -e '\e[1;31mFailed\e[0m'{{ end }}"
      - task: lint
      - task: formatting
      - task: validate
      - task: analyse
      - task: documentation
      - cmd: echo -e '\e[0;32mCompleted\e[0m'

  formatting:
    aliases:
      - f
    desc: Format the selected files within this repository
    summary: |-
      Re-format the selected files within this repository to ensure they are consistent in their layout and usage,
      including running `terraform fmt` for all Terraform files, and running all Markdown, YAML, JSON, and TOML files
      through Prettier.
    silent: true
    cmds:
      - defer: "{{ if .EXIT_CODE }}echo -e '\e[1;31mFailed\e[0m'{{ end }}"
      - task: prettier
      - task: terraform:fmt
      - cmd: echo -e '\e[0;32mCompleted\e[0m'

  lint:
    aliases:
      - l
    desc: Lint selected files within this repository
    summary: |-
      Run linting checks across all Terraform, YAML, and Markdown files to find any potential issues or inconsistencies
      within the code.
    silent: true
    cmds:
      - defer: "{{ if .EXIT_CODE }}echo -e '\e[1;31mFailed\e[0m'{{ end }}"
      - task: tflint
      - task: yamllint
      - task: markdownlint
      - cmd: echo -e '\e[0;32mCompleted\e[0m'

  validate:
    desc: Validate selected files within this repository
    summary: |-
      Run validation checks on all Terraform files, as well as selected YAML files, including GitHub Workflows, GitHub
      Actions, the GitHub Dependabot configuration, Mergify, Taskfiles, and the pre-commit configuration.
    silent: true
    cmds:
      - defer: "{{ if .EXIT_CODE }}echo -e '\e[1;31mFailed\e[0m'{{ end }}"
      - task: check-jsonschema
      - task: terraform:validate
      - task: terraform:lock
      - cmd: echo -e '\e[0;32mCompleted\e[0m'

  analyse:
    desc: Run static analysis checks for this repository
    summary: |-
      Run static code analysis tools (i.e Snyk) against the Terraform configuration, as well zizmor and actionlint
      against the GitHub Dependabot configuration, all GitHub Workflow files, and the GitHub Action configuration, to
      identify any potential security issues and misconfigurations within the code.
    silent: true
    cmds:
      - defer: "{{ if .EXIT_CODE }}echo -e '\e[1;31mFailed\e[0m'{{ end }}"
      - task: snyk
      - task: actionlint
      - task: zizmor
      - cmd: echo -e '\e[0;32mCompleted\e[0m'

  documentation:
    aliases:
      - docs
    desc: Update README.md files with terraform-docs
    summary: |-
      Update the README.md files for the Terraform configurations with a summary of the configuration, including
      information about the requirements, variables and outputs, as well as the resources created, using terraform-docs.
    silent: true
    cmds:
      - defer: "{{ if .EXIT_CODE }}echo -e '\e[1;31mFailed\e[0m'{{ end }}"
      - task: terraform:documentation
      - cmd: echo -e '\e[0;32mCompleted\e[0m'

  clean:
    aliases:
      - c
    desc: Clean temporary files from the repository and configurations
    summary: |-
      Clean any temporary directories and files created by both this Taskfile, and the tools and applications called
      from it, and from within the configurations.
    run: once
    silent: true
    cmds:
      - defer: "{{ if .EXIT_CODE }}echo -e '\e[1;31mFailed\e[0m'{{ end }}"
      - task: terraform:clean
      - cmd: rm -f .prettiercache
      - cmd: rm -rf .task/checksum
      - cmd: echo -e '\e[0;32mCompleted\e[0m'
